name: "Cache MJ Database"

on:
  schedule:
    - cron: "0 3 1 */3 *"

  workflow_dispatch:

jobs:
  download-mj-database:
    runs-on: self-hosted
    steps:
      - name: Debug CI
        run: |
          ip route get 85.120.166.106
          curl -k -sS -o /dev/null --max-time 30 -w 'local=%{local_ip} code=%{http_code}\n' https://www.just.ro/

